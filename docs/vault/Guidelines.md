# Guidelines

Use this page for contribution rules, naming conventions, review expectations, and release habits.

Recommended token naming:

- Primitive tokens describe raw scales, such as `neutral/900`.
- Semantic tokens describe usage, such as `text/default`.
- Component tokens describe a component-specific role only when a semantic token is too broad.
