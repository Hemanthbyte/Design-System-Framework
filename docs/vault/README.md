# Design System Vault

This folder is the editable documentation source.

Obsidian is recommended because it gives designers a friendly writing surface over plain markdown files. Any markdown editor works.

Write prose here. Generated token references live in `docs/generated/tokens/` and are promoted into the site output by script.
